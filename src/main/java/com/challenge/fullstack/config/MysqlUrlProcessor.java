package com.challenge.fullstack.config;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.env.EnvironmentPostProcessor;
import org.springframework.core.env.ConfigurableEnvironment;

//public class MysqlUrlProcessor implements EnvironmentPostProcessor {

  //  @Override
    //public void postProcessEnvironment(ConfigurableEnvironment environment, SpringApplication application) {
    //    String mysqlUrl = environment.getProperty("MYSQL_URL");
      //  if (mysqlUrl != null && mysqlUrl.startsWith("mysql://")) {
            // Convertir al formato JDBC
        //    String jdbcUrl = "jdbc:" + mysqlUrl;
        //    System.setProperty("MYSQL_URL", jdbcUrl);
       // }
    //}
//}